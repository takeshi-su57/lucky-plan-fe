"use client";

/**
 * @deprecated Pipeline configuration is now shown contextually within each wizard step.
 * This component is kept as a stub for backwards compatibility.
 */
export function PipelineConfigTab() {
  return (
    <div className="flex flex-col items-center justify-center gap-2 py-12 text-neutral-400">
      <p>Configuration is now managed within each wizard step.</p>
    </div>
  );
}
